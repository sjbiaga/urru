package urru
package game
package flow

import scala.annotation.experimental
import org.scalatest.funsuite.AnyFunSuite
import org.scalamock.scalatest.MockFactory

import common.grid.x

import tense.intensional.Data.Doubt

import Clue.Start

import urru.grid.Grid.Id
import grid.Game.Feature.*


@experimental
class PathSuite extends AnyFunSuite with MockFactory:

  test("") {

    // val game = Game(1, 9 x 9, Set(Start(1 x 1, 9 x 9, -1), Start(9 x 9, 1 x 1, -1)), Just)

    // val path = Path(Id(1), 0, 1)

    // var move: Move = null

    // val in = Doubt(Set.empty)

    // move = Move(0, 0 x 0, 0 x 1, -1, 0)

    // path(move)(in)

  }

